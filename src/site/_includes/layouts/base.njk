<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>{% include "css/main.css" %}</style>
    <title>{{ title }}</title>
  </head>
  <body class="{{ bodyClass }}" >
    <div class="container">
      <header>
        <h1>{{ title }}</h1>
        {%- if subtitle %}<p class="subtitle">{{ subtitle | md | safe }}</p>{% endif %}
      </header>
      {{ content | safe }}
      {% include "footer.njk" %}
      <script>{% include "js/scripts.js" %}</script>
    </div>
  </body>
</html>



